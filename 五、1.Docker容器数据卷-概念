1.Docker容器在关闭之后，容器上的数据会被清空。
  为了能够保存容器中的数据，在Docker中使用数据卷。
  
2.特点 ：
  2.1 数据卷可在容器之间共享或重用数据；
  2.2 卷中的更改可以直接生效；
  2.3 数据卷的更改不会包含在镜像更新中；
  2.4 数据卷的生命周期一直持续到没有容器使用它为止。
  
3.效果 ：
  可以实现主机到容器，容器到主机之间的数据共享。
