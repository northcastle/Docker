1.镜像是什么？
【答】 ： 镜像是一种轻量级、可执行的独立软件包，
         用来打包软件运行环境和基于运行环境开发的软件.
         它包含运行某个软件所需的所有内容，包括代码，运行时环境变量和配置文件.
         
2.Union FS 联合文件系统
  对文件系统的修改作为一次提交来一层层地叠加，这是docker的镜像的基础.
  【特性】一次同时加载多个文件系统。但对外，只能看到一个文件系统。
  
3.镜像都是只读的.

